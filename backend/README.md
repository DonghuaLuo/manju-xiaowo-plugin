# Manju Backend

这是 Manju 小蜗插件的 Python 后端，保留媒体生成、项目数据、任务队列和 Agent Runtime 等运行代码。

后端运行在小蜗插件体系内，不再包含原 ArcReel WebUI、Docker 部署、GitHub Actions、旧发布自动化、OpenSpec 或旧站点文档。

项目数据目录 `projects/` 是本地运行数据，不属于清理范围。
