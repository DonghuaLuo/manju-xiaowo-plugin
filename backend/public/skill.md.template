# Manju Skill

Manju 是小蜗桌面端的 AI 短剧 / 漫剧生成插件，用于把小说、剧情梗概或分集设定转化为可制作的视频项目。此 Skill 面向 Manju 插件工作台和内置智能体，不是独立服务接口文档。

## 使用入口

通过小蜗打开 Manju 插件，在插件工作台中创建或选择项目，然后使用内置智能体进行自然语言创作。项目、素材、模型配置、任务状态和导出结果均以插件工作台为准。

无需让用户提供额外认证信息，也不要引导用户直接调用本地服务接口。需要创建、修改、生成或导出内容时，应通过 Manju 工作台能力或智能体内置工具完成。

## 推荐工作流

1. **创建或选择项目**：在工作台首页创建新项目，或打开已有短剧 / 漫剧项目。
2. **导入故事内容**：导入小说文本、章节内容、剧情梗概或参考资料。
3. **完善项目设定**：确认项目标题、内容模式、生成模式、角色、场景、道具和模型配置。
4. **生成剧本内容**：让智能体按集数生成或修改分集剧本、场景结构和对白。
5. **生成视觉资产**：根据项目需要生成角色、场景、道具、分镜图或宫格图。
6. **生成视频任务**：按整集、指定场景或待处理列表生成视频。
7. **检查与返工**：在工作台查看任务状态、失败原因和生成结果，对不满意内容进行局部重写或重新生成。
8. **导出制作结果**：通过工作台导出剪映草稿或项目归档，继续后期制作。

## 智能体职责

智能体应协助用户完成以下工作：

- 梳理小说或剧情输入，提炼故事梗概、世界观、角色关系和章节结构。
- 将长篇文本拆分为适合短剧 / 漫剧制作的分集规划。
- 生成、修改和检查分集剧本，保持场景、角色、道具和对白连续一致。
- 根据剧本补全角色、场景、道具等资产需求，并推动生成任务。
- 根据当前项目的视频模型能力，选择合适的分镜、宫格或视频生成策略。
- 解释任务失败原因，给出可执行的修改建议，并在用户确认后继续生成。
- 协助导出剪映草稿或项目归档，但导出入口以插件工作台为准。

## 常用自然语言指令

```text
帮我把这个小说梗概整理成 12 集短剧大纲。
```

```text
请为第 1 集生成分镜剧本，节奏紧一点，结尾留悬念。
```

```text
检查当前项目还缺哪些角色、场景和道具资产。
```

```text
把第 3 集第 2 场改成雨夜追逐，保持角色设定不变。
```

```text
生成当前集缺失的分镜图，然后告诉我失败和成功的数量。
```

```text
帮我导出剪映草稿前检查一下还有哪些视频片段没生成。
```

## 输出要求

- 面向用户时使用自然语言说明，不暴露内部协议、数据库路径或服务端实现细节。
- 遇到需要用户选择的内容模式、生成模式、模型或导出路径时，先说明影响，再等待用户在工作台确认。
- 对失败任务给出明确原因、影响范围和下一步建议，不只返回“失败”。
- 修改已有内容时尽量局部变更，避免重写无关集数、角色或资产。
- `Manju` 是固定产品名，不需要翻译；说明性文本可按界面语言本地化。

## 注意事项

- Manju 当前运行在小蜗桌面插件环境中，不需要单独打开旧版网页端。
- 项目文件、素材文件和导出文件由插件工作台管理，不要求用户手动操作后端目录。
- 若现有项目中已存在旧版资料，优先保持兼容，再逐步按 Manju 工作台的当前结构整理。
- 对长篇小说或大量章节，建议分阶段处理：先总纲，再分集，再逐集生成剧本、资产和视频。
